import { useEffect } from 'react';
// import { useSelector } from 'react-redux';

interface ISecureContextProps {}

export const SecureContext = (props: ISecureContextProps) => {
  // const [isAuthenticated, setIsAuthenticated] = useState();
  useEffect(() => {});
};

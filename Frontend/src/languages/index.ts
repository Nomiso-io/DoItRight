import en from './en.json';
import de from './de.json';
import sp from './sp.json';

export const dictionaryList = { en, de, sp };

export const languageOptions = {
  en: 'English',
  de: 'Deutsch',
  sp: 'Española',
};
